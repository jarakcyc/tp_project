Декоратор:
RelaxDecorator, BattleDecorator (функция update()) 
для Warrior 

Исходные код: army/army.cpp, army/army.h

В состоянии отдыха у юнитов восстанавливается здоровье, в бою ничего не происходит

Прокси:
ProxyInvoker, которые перехватывает запросы пользователя, парсит их 
и дальше передает параметры invoker, который исполняет соответствующую команду

Исходный код: gameplay.h, gameplay.cpp